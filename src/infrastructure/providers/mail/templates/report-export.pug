extends layouts/base

block label
  | Relatório

block title
  | #{reportLabel}

block content
  if name
    p(style="margin:0 0 16px 0;") #{name},
  p(style="margin:0 0 16px 0;") Segue em anexo o relatório solicitado (#{filename}).
  p(style="margin:0;") Este arquivo contém os dados consolidados do relatório selecionado.

block cta
